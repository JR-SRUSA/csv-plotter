PROMPT
======

When hover data turned off, the track marker no longer follows mouse position, it should even when hover data is off.

Let's move the "Pin on Map" and "Pin on Graph" buttons down by where we're actually entering the note.

Let's wrap the note text when its too wide to show either on the map or graph hover view. 

When we hover on a note that is linked with the graph or map, can we show that icon as higlighted? Like an icons hover effect.

In the sessions tab, Event Dropdown should be at the top, then session.

Under the User Tab, let's have an Authorname slot, which we can use as the "local" user (no login system yet implemented)

For the bike, power should be in kW, not W. and we should have the option to enter either RPM, kW or RPM, Nm (torque). Can we put up a Torque/ Power vs. RPM graph in the "edit bike" when the user puts in torque/ power numbers? 

I'd also like to be able to load an "event" or "session" directly, rather than just the CSV file. When I click "Pick Uploaded Data" that should be possible.


PROMPT
======
Let's allow the torque/ power graph to be a bit taller when it plots (its smooshed now), and torque and power can share the same Y-axis (typical in industry)


Did you actually fix the vehicle power graph/ display before? It still looks smooshed

Can we add the take/ attach photo button to the popup map note interface as well?

When we delete something (like a comment, or datafile) lets put up a toast that says its deleted with an "undo" option. 
